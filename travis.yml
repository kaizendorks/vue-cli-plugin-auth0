language: node_js
node_js: "stable"

services:
  - docker

install:
  - docker build -t auth0 .
  - yarn install

script:
  - yarn eslint

deploy:
  provider: script
  script:
    - npm publish
  skip_cleanup: true
  on:
    branch: master
